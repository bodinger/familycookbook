<%- actions2([
  remove_link('Cancel', :href => url(:ingredient_type, :index), :class => 'btn btn-danger'),
  '&nbsp;'.html_safe,
  checked_link('Update', :href => '#', :id => 'ingredient-type-form-'+@ingredient_type.id.to_s+'-button', :class => 'btn btn-success')
]) %>

<%- form_for :ingredient_type, url(:ingredient_type, :update, :id => @ingredient_type.id), :method => :put, :class => 'form-horizontal', :id => 'ingredient_type' do |f| %>
  <%= render_partial 'ingredient_type/form_fields', :object => f %>
  <%= f.submit 'Update', :class => 'btn btn-primary hidden', :id => 'ingredient-type-form-'+@ingredient_type.id.to_s+'-submit' %>
<% end %>

<%= render_partial 'ingredient_type/list_used_in', :object => @tag, :locals => { :action => :edit } %>

<%= render_partial 'shared/js_bind_submit', :locals => { :object => @ingredient_type, :object_name => 'ingredient-type', :action => :edit } %>
