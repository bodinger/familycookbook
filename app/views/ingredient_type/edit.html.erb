<%- actions({ 'Cancel' => url(:ingredient_type, :index) }) %>

<%- form_for :ingredient_type, url(:ingredient_type, :update, :id => @ingredient_type.id), :method => :put, :class => 'form-horizontal', :id => 'ingredient_type' do |f| %>
  <%= render_partial 'ingredient_type/form_fields', :object => f %>
  <%= f.submit 'Update', :class => 'btn btn-primary' %>
<% end %>

