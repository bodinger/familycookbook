<%- actions({ 'Cancel' => url(:unit, :index) }) %>

<%- form_for :unit, url(:unit, :create), :method => :post, :class => 'form-horizontal' do |f| %>
  <%= render_partial 'unit/form_fields', :object => f %>
  <%= f.submit 'Create', :class => 'btn btn-primary' %>
<% end %>