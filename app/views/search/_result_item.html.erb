<div class="row row-pd row-<%= row_class(index) %>">
  <% @result.table_config[:body].each do |column| %>
  <div class="col-md-<%= column[:width] %>"><%= link_to item.send(column[:field]), url(result.controller, result.action, item.id) %></div>
  <% end %>
</div>
