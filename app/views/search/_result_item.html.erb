<div class="row row-pd row-<%= row_class(index) %>">
  <% table_config[:body].each do |column| %>
  <div class="col-md-<%= column[:width] %>"><%= link_to item.send(column[:field]), url(controller[:controller], controller[:action], item.id) %></div>
  <% end %>
</div>
