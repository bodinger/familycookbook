<%- f = form_fields %>

<%= error_messages { f.error_messages } %>
<%= form_group(f, :title) %>
<%= form_group(f, :description) %>

<%= form_group(f, :type) %>

<%= form_group(f, :diffculty) %>
<%= form_group(f, :duration_cooking) %>
<%= form_group(f, :duration_preparation) %>
<%= form_group(f, :cookware_amount) %>

<%= form_group(f, :tips_and_tricks) %>
